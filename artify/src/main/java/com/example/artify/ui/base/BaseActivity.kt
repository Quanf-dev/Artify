package com.example.artify.ui.base

import android.content.Context
import androidx.appcompat.app.AppCompatActivity
import com.example.artify.utils.LocaleHelper

/**
 * Base Activity class that handles language changes for all activities
 */
abstract class BaseActivity : AppCompatActivity() {

    /**
     * Attach the correct base context with the saved language
     */

} 